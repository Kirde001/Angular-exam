/**
 * Интерфейс категории каталога
 *
 * @property {number} id - идентификатор категории
 * @property {string} name - наименование категории
 */
export interface CatalogCategory {
  id: number;
  name: string;
}
